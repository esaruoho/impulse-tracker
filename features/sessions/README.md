# Convey — per-session distillation stubs

Auto-written by the SessionEnd distiller (`../convey-distill.py`) when a Convey
conversation ends: one `<session-id>.md` stub per session — metadata + touched
cards/tools + `claude --resume` command + `@distill-pending`. No dialogue text.

Promote a stub to a full `<name>.session.md` vibe-diff when it holds decisions
worth keeping. `.distill-log` is local proof-of-fire (gitignored).
