# Duplicate scenarios — pruned from the hardware walk

> Generated by `features/hwtest.py`. Each entry is a scenario you judged a redundant duplicate (tagged `@duplicate` in its card, dropped from the walk). Convey-prune: a malformed/redundant branch, recorded WITH a reason — never a silent delete.

_No duplicates marked._

