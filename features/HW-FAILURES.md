# Hardware test FAILURES — send these back

> Generated by `features/hwtest.py`. Each entry is a fork scenario that FAILED on real hardware, with your note. This is the focused worklist.


## 🔴 `wav-render-reentry-guard` — Right starts the render, Shift-Right during it halts and finalizes
**How it failed:** i was pressing right, then shift-right, then right, then right right shift-right and it finished with "Finished writing file", but the UI became unresponsive. needs a fix.


## 🔴 `wav-render-reentry-guard` — Early-stop reuses the existing safe finalize, not a new teardown
**How it failed:** described perviously


## 🔴 `alt-r-replicate` — Alt-R and Shift-Alt-R are disambiguated by live shift state
**How it failed:** q

